# https://github.com/graphql-java/graphql-java/releases/tag/v9.0

type User {
  id: ID!
  name: String!
}

type Rating {
  id: ID!
  user: User!
  stars: Int!
}

type Beer {
  id: ID!
  name: String!
  price: String!

  ratings: [Rating!]!
}

type Query {
  beers: [Beer!]!
}